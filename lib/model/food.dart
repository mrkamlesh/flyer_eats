class Food {
  final String title;
  final String description;
  final int price;
  final String image;
  final bool isAvailable;

  Food(this.title, this.description, this.price, this.image, this.isAvailable);
}
